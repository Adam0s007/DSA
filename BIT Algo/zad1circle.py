# Mamy dane n punktow (x,y) w okregu o promieniu k(liczba naturalna),
# tzn 0 <= x^2 + y^2 <= k, ktore są w nim rownomiernie rozlozone, tzn.
# prawdopodobienstwo znalezienia punktu na danym obszarze jest proporcjonalne do pola tego osbzaru.
# Napisz algorytm, ktory w czasie teta(n) posortuje punkty po ich odleglosci do punktu (0,0), tzn. 
# d = sqrt(x^2 + y^2)

